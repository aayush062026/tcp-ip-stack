# Architecture - TCP/IP Stack

## Overview

This document describes the block-level architecture of the TCP/IP stack reference design, covering fundamental packet processing across the network and transport layers.

## Status

Work in progress placeholder. To be completed with details on the IP layer (addressing, checksum, fragmentation), the TCP layer (connection state machine, sequence/ack handling), and the packet processing datapath.

## Sections to cover

Top-level block diagram and module hierarchy. IP header processing and checksum. TCP connection state machine. Packet buffering and processing datapath. Verification and testbench approach.
